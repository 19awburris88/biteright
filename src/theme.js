// src/theme.js
import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#D35400', // Burnt orange
    },
    background: {
      default: '#FFF8F0',
    },
    text: {
      primary: '#1A1A1A',
    },
    secondary: {
      main: '#F5B041', // Accent gold
    },
  },
});
